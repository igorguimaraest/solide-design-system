{
  "name": "solide-design-system-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Ecossistema digital da marca Solide — Design Tokens, UI Kit e Documentação Técnica",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npx http-server . -p 8080 -c-1 -o /solide-brand-guide.html",
    "serve": "npx http-server . -p 8080 -c-1",
    "build:tokens": "npm run build --workspace=@solide/tokens",
    "build:ui": "npm run build --workspace=@solide/ui-kit",
    "build": "npm run build:tokens && npm run build:ui && npm run build:preview",
    "typecheck": "npm run typecheck --workspace=@solide/ui-kit",
    "test:tokens": "npm run build:tokens && node packages/tokens/build/verify-tokens.js && git diff --exit-code -- packages/tokens/src/mobile-theme.ts packages/tokens/src/primitives.tokens.json packages/tokens/src/semantic.tokens.json docs/consolidation/contrast.json packages/tokens/src/motion.ts",
    "storybook": "storybook dev -p 6006",
    "build:storybook": "storybook build",
    "preview:ui": "vite --host 127.0.0.1",
    "test:ui": "playwright test",
    "lint": "node scripts/audit-system.js --check",
    "build:preview": "vite build"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@storybook/react-vite": "^10.6.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "@vitejs/plugin-react": "^6.1.1",
    "autoprefixer": "^10.5.6",
    "postcss": "^8.5.28",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "storybook": "^10.6.0",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.4.5",
    "vite": "^8.3.0"
  }
}
